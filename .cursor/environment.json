{
  "name": "github-slideshow (Jekyll)",
  "user": "ubuntu",
  "install": "bash .cursor/install.sh",
  "terminals": [
    {
      "name": "jekyll serve",
      "command": "export PATH=\"$HOME/.rbenv/shims:$HOME/.rbenv/bin:$PATH\"; exec bundle exec jekyll serve --host 0.0.0.0 --port 4000 --livereload"
    }
  ],
  "ports": [4000]
}
